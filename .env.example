# OpenRouter API Key (required)
# Get your key from https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-...

# LLM Models (required)
# These are free models available on OpenRouter. You can use any model.
# The ":free" suffix indicates free-tier models (lower rate limits).
# See https://openrouter.ai/models for available options.
GENERATOR_MODEL=nvidia/nemotron-3-nano-30b-a3b:free
JUDGE_MODEL=tngtech/deepseek-r1t2-chimera:free

# Server port (optional, defaults to 3001)
PORT=3001

# PokéAPI Base URL (optional, defaults to official API)
POKEAPI_BASE_URL=https://pokeapi.co/api/v2

# CORS allowed origins for production (optional)
# Comma-separated list of allowed origins. In development, all origins are allowed.
# ALLOWED_ORIGINS=https://example.com,https://app.example.com
